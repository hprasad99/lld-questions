class DisplayBoard {
    #id;
    #parkingSpots;
    constructor(id) {
        this.#id = id;
        this.#parkingSpots = new Map();
    }

    addParkingSpot(spotType , spots) {}
    showFreeSlot() {}
}